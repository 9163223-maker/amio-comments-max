'use strict';

const store = require('./store');
const tenant = require('./tenant-scope');

const RUNTIME = 'CC8.1.0-CLEAN-GIFTS-BUTTONS-TENANT-FOUNDATION';
const MAX_POSTS = 8;
const CLEAN_BUTTON_ACTIONS = [
  'admin_section_buttons', 'button_admin_recent_posts', 'button_admin_channel_pick', 'button_admin_select_post', 'button_admin_show_current',
  'button_admin_start_add', 'button_admin_save', 'button_admin_cancel', 'button_admin_delete', 'button_admin_reorder_up', 'button_admin_reorder_down'
];
function clean(value) { return String(value || '').trim(); }
function n(value) { const num = Number(value || 0); return Number.isFinite(num) ? num : 0; }
function array(value) { return Array.isArray(value) ? value : []; }
function safeCall(fn, fallback) { try { return fn(); } catch { return fallback; } }
function short(value, max = 72) { const s = clean(value).replace(/\s+/g, ' '); return s.length <= max ? s : s.slice(0, Math.max(1, max - 1)).trim() + '…'; }
function button(menu, text, action, extra) { return menu.button(text, action, extra || {}); }
function keyboard(menu, rows) { return menu.keyboard(rows); }
function footer(menu) { return [[button(menu, '🔘 В начало кнопок', 'admin_section_buttons')], [button(menu, '🏠 Главное меню', 'admin_section_main')]]; }
function screen(menu, id, title, lines, rows) { return { id, text: [title, '', ...(lines || [])].filter(Boolean).join('\n'), attachments: keyboard(menu, rows || footer(menu)) }; }
function getTenant(userId = '') { return tenant.ensureTenantContext(userId); }
function getSetup(userId = '') { getTenant(userId); return safeCall(() => store.getSetupState(clean(userId)), {}) || {}; }
function channelTitle(post = {}) { return clean(post.channelTitle || post.channelName || post.chatTitle || post.title || post.name || post.channelId || 'Канал'); }
function postTitle(post = {}) { return short(post.originalText || post.postText || post.text || post.caption || post.postId || post.messageId || post.commentKey || 'Пост без текста', 58); }
function postTime(post = {}) { const ts = n(post.updatedAt || post.createdAt || post.ts || 0); if (!ts) return ''; try { return new Date(ts).toISOString().slice(0, 16).replace('T', ' '); } catch { return ''; } }
function targetRecord(post = {}, userId = '') { return tenant.stampRecord({ channelId: clean(post.channelId), channelTitle: channelTitle(post), postId: clean(post.postId), messageId: clean(post.messageId), commentKey: clean(post.commentKey), originalText: clean(post.originalText || post.postText || post.text || ''), linkedAt: Date.now() }, getTenant(userId), post); }
function findPost(commentKey = '', userId = '') { const key = clean(commentKey); if (!key) return null; const post = safeCall(() => store.getPost(key), null) || safeCall(() => array(store.getPostsList()).find((item) => clean(item && item.commentKey) === key), null) || null; return post && tenant.belongsToTenant(post, getTenant(userId)) ? post : null; }
function listAllPosts(userId = '') { const ctx = getTenant(userId); const seen = new Set(); return safeCall(() => array(store.getPostsList()), []).filter((post) => post && clean(post.commentKey) && clean(post.channelId) && clean(post.postId) && tenant.belongsToTenant(post, ctx)).filter((post) => { const key = clean(post.commentKey); if (!key || seen.has(key)) return false; seen.add(key); return true; }).sort((a, b) => n(b.updatedAt || b.createdAt || b.ts) - n(a.updatedAt || a.createdAt || a.ts)); }
function listPosts(channelId = '', userId = '') { const channel = clean(channelId); return listAllPosts(userId).filter((post) => !channel || clean(post.channelId) === channel).slice(0, MAX_POSTS); }
function listChannelsFromPosts(userId = '') { const map = new Map(); listAllPosts(userId).forEach((post) => { const id = clean(post.channelId); if (!id || map.has(id)) return; map.set(id, channelTitle(post)); }); return Array.from(map.entries()).map(([channelId, title]) => ({ channelId, title })); }
function getStoredTarget(userId = '') { const state = getSetup(userId); const target = state.buttonTargetPost || state.commentTargetPost || null; const post = target && target.commentKey ? findPost(target.commentKey, userId) : null; return post || target || null; }
function bindTarget(userId = '', post = {}, options = {}) { const uid = clean(userId); const target = targetRecord(post, uid); if (!uid || !target.commentKey) return target; safeCall(() => { const prev = getSetup(uid); const adminUi = { ...(prev.adminUi || {}), section: 'buttons', backAction: 'admin_section_buttons', rootAction: 'admin_section_buttons', selectMode: 'buttons' }; const patch = { tenantKey: target.tenantKey, ownerUserId: target.ownerUserId, buttonTargetPost: target, commentTargetPost: target, adminUi, activeAdminUi: adminUi }; if (!options.keepFlow) { patch.buttonFlow = null; patch.activeAdminFlowKind = ''; } store.setSetupState(uid, patch); }, null); return target; }
function channelStore(channelId = '') { const id = clean(channelId); if (!store.store.growth) store.store.growth = { byChannel: {}, clicks: [], pollVotes: [], memberSnapshots: {} }; if (!store.store.growth.byChannel) store.store.growth.byChannel = {}; if (!store.store.growth.byChannel[id]) store.store.growth.byChannel[id] = { channelId: id }; if (!store.store.growth.byChannel[id].buttonSets) store.store.growth.byChannel[id].buttonSets = {}; return store.store.growth.byChannel[id]; }
function getButtonSet(target = null, userId = '') { if (!target || !target.channelId || !target.commentKey) return []; const ctx = getTenant(userId); const set = channelStore(target.channelId).buttonSets[clean(target.commentKey)] || []; return array(set).filter((item) => tenant.belongsToTenant(item, ctx)); }
function saveButtonSet(target = null, buttons = [], userId = '') {
  if (!target || !target.channelId || !target.commentKey) return [];
  const ctx = getTenant(userId);
  const key = clean(target.commentKey);
  const stamped = array(buttons).map((item, index) => tenant.stampRecord({ ...item, id: clean(item.id) || `btn_${index + 1}`, postIds: [clean(target.postId)].filter(Boolean), commentKey: key, channelId: clean(target.channelId), order: index + 1 }, ctx, item));
  const bucket = channelStore(target.channelId);
  const existing = array(bucket.buttonSets[key]);
  const otherTenants = existing.filter((item) => !tenant.belongsToTenant(item, ctx));
  bucket.buttonSets[key] = [...otherTenants, ...stamped];
  safeCall(() => store.saveStore(store.store), null);
  return stamped;
}
function setButtonFlow(userId = '', flow = null) { const uid = clean(userId); if (!uid) return null; const ctx = getTenant(uid); safeCall(() => { const prev = getSetup(uid); const adminUi = { ...(prev.adminUi || {}), section: 'buttons', backAction: 'admin_section_buttons', rootAction: 'admin_section_buttons', selectMode: 'buttons' }; store.setSetupState(uid, { tenantKey: ctx.tenantKey, ownerUserId: ctx.ownerUserId, buttonFlow: flow, giftFlow: null, commentAdminFlow: null, postEditFlow: null, activeAdminFlowKind: flow ? 'button' : '', adminUi, activeAdminUi: adminUi }); }, null); return flow; }
function targetLines(target = null) { if (!target || !target.commentKey) return ['Пост пока не выбран.']; return [`Канал: ${channelTitle(target)}`, `Пост: ${postTitle(target)}`, `Post ID: ${short(target.postId || '—', 80)}`]; }
function buttonsLines(buttons = []) { if (!buttons.length) return ['Кнопки для выбранного поста пока не созданы.']; return buttons.map((item, index) => `${index + 1}. ${clean(item.text || 'Кнопка')} → ${short(item.url || item.action || 'без ссылки', 90)}`); }
function homeRows(menu, target = null, flow = null, userId = '') { const buttons = getButtonSet(target, userId); const rows = []; if (flow) rows.push([button(menu, '❌ Отменить черновик', 'button_admin_cancel')]); else if (target && target.commentKey) { rows.push([button(menu, '➕ Добавить кнопку', 'button_admin_start_add')]); if (buttons.length) rows.push([button(menu, '🗑 Удалить последнюю кнопку', 'button_admin_delete')]); rows.push([button(menu, '📋 Текущие кнопки', 'button_admin_show_current')]); rows.push([button(menu, '📌 Выбрать другой пост', 'button_admin_recent_posts', { page: 0 })]); } else { rows.push([button(menu, '📌 Выбрать пост для кнопок', 'button_admin_recent_posts', { page: 0 })]); rows.push([button(menu, '📋 Текущие кнопки', 'button_admin_show_current')]); } rows.push([button(menu, '🏠 Главное меню', 'admin_section_main')]); return rows; }
async function home(menu, payload = {}, ctx = {}) { const state = getSetup(ctx.userId); const target = getStoredTarget(ctx.userId); const flow = state.buttonFlow || null; const buttons = getButtonSet(target, ctx.userId); const lines = ['Clean Core экран CTA / пользовательских кнопок.', 'Старую дублирующую CTA-логику больше не используем для этого входа.', '', ...targetLines(target), '', ...buttonsLines(buttons), '', 'Сохранение кнопок tenant-scoped: новый клиент не увидит чужие кнопки.']; if (payload.note) lines.unshift(clean(payload.note), ''); return screen(menu, 'buttons_clean_home', '🔘 CTA / пользовательские кнопки', lines, homeRows(menu, target, flow, ctx.userId)); }
async function channelPicker(menu, payload = {}, ctx = {}) { const channels = listChannelsFromPosts(ctx.userId).slice(0, 12); const rows = channels.map((item, index) => [button(menu, `${index + 1}. ${short(item.title || item.channelId, 52)}`, 'button_admin_channel_pick', { channelId: item.channelId })]); if (!rows.length) rows.push([button(menu, 'Пока нет каналов в памяти', 'admin_section_buttons')]); rows.push(...footer(menu)); return screen(menu, 'buttons_clean_channel_picker', '📺 Канал для кнопок', ['Выберите канал. Список tenant-scoped.'], rows); }
async function picker(menu, payload = {}, ctx = {}) { const page = Math.max(0, Number(payload.page || 0)); const channelId = clean(payload.channelId || ''); if (!channelId && listChannelsFromPosts(ctx.userId).length > 1 && clean(payload.skipChannels || '') !== '1') return channelPicker(menu, payload, ctx); const posts = listPosts(channelId, ctx.userId); const rows = posts.map((post, index) => [button(menu, `${index + 1 + page * MAX_POSTS}. ${postTitle(post)}`, 'button_admin_select_post', { commentKey: clean(post.commentKey), channelId })]); if (!rows.length) rows.push([button(menu, 'Пока нет постов в памяти', 'admin_section_buttons')]); rows.push(...footer(menu)); const lines = [channelId ? `Канал: ${channelTitle(posts[0] || { channelId })}` : 'Выберите пост из последних сохранённых постов.', 'Список строится из store/cache без live-запросов к MAX.']; if (posts.length) { lines.push(''); posts.forEach((post, index) => { const meta = [channelTitle(post), postTime(post)].filter(Boolean).join(' · '); lines.push(`${index + 1}. ${postTitle(post)}${meta ? '\n   ' + meta : ''}`); }); } return screen(menu, 'buttons_clean_picker', '📌 Выбор поста для кнопок', lines, rows); }
async function selectPost(menu, payload = {}, ctx = {}) { const post = findPost(payload.commentKey || '', ctx.userId); if (!post || !post.commentKey) return screen(menu, 'buttons_clean_not_found', '🔘 CTA / пользовательские кнопки', ['Пост не найден в текущем tenant/store.'], [[button(menu, '📌 Выбрать пост', 'button_admin_recent_posts', { page: 0 })], ...footer(menu)]); bindTarget(ctx.userId, post); return home(menu, { note: 'Пост для кнопок выбран.' }, ctx); }
async function showCurrent(menu, payload = {}, ctx = {}) { const target = getStoredTarget(ctx.userId); const buttons = getButtonSet(target, ctx.userId); return screen(menu, 'buttons_clean_current', '📋 Текущие кнопки', [...targetLines(target), '', ...buttonsLines(buttons)], homeRows(menu, target, null, ctx.userId)); }
async function startAdd(menu, payload = {}, ctx = {}) { const target = getStoredTarget(ctx.userId); if (!target || !target.commentKey) return picker(menu, { page: 0 }, ctx); const flow = tenant.stampRecord({ mode: 'button_wizard', stepIndex: 0, targetPost: target, draft: { id: `btn_${Date.now().toString(36)}`, text: '', url: '', style: 'primary' }, runtimeVersion: RUNTIME }, getTenant(ctx.userId)); setButtonFlow(ctx.userId, flow); return screen(menu, 'buttons_clean_add_label', '➕ Добавление кнопки', ['Шаг 1/2. Напишите текст кнопки.', '', ...targetLines(target), '', 'Например: Записаться / Получить консультацию / Открыть сайт'], [[button(menu, '❌ Отменить', 'button_admin_cancel')], [button(menu, '🏠 Главное меню', 'admin_section_main')]]); }
async function saveDraft(menu, ctx = {}) { const state = getSetup(ctx.userId); const flow = state.buttonFlow || null; const target = flow?.targetPost || getStoredTarget(ctx.userId); if (!flow || !flow.draft || !target) return home(menu, { note: 'Черновик кнопки не найден.' }, ctx); const existing = getButtonSet(target, ctx.userId); const next = [...existing, flow.draft]; saveButtonSet(target, next, ctx.userId); setButtonFlow(ctx.userId, null); return home(menu, { note: 'Кнопка сохранена. Repatch поста будет выполняться отдельным clean-save этапом, не при простом открытии.' }, ctx); }
async function removeLast(menu, payload = {}, ctx = {}) { const target = getStoredTarget(ctx.userId); const existing = getButtonSet(target, ctx.userId); if (!existing.length) return home(menu, { note: 'Кнопок для удаления нет.' }, ctx); existing.pop(); saveButtonSet(target, existing, ctx.userId); return home(menu, { note: 'Последняя кнопка удалена. Repatch поста будет выполняться отдельным clean-delete этапом.' }, ctx); }
async function cancel(menu, payload = {}, ctx = {}) { setButtonFlow(ctx.userId, null); return home(menu, { note: 'Черновик кнопки отменён. Чужие меню не очищались.' }, ctx); }
function extractUrl(value = '') { const m = clean(value).match(/https?:\/\/\S+/i); return m ? m[0].replace(/[)\],.]+$/, '') : ''; }
async function handleTextInput(menu, ctx = {}) { const userId = clean(ctx.userId); const state = getSetup(userId); const flow = state.buttonFlow || null; if (!flow || clean(state.activeAdminFlowKind) !== 'button') return null; const input = clean(ctx.text || ''); const draft = flow.draft || {}; if (Number(flow.stepIndex || 0) <= 0) { if (!input) return null; draft.text = input.slice(0, 64); flow.draft = draft; flow.stepIndex = 1; setButtonFlow(userId, flow); return screen(menu, 'buttons_clean_add_url', '➕ Добавление кнопки', ['Шаг 2/2. Пришлите ссылку для кнопки.', '', `Текст: ${draft.text}`, '', 'Ссылка должна начинаться с https://'], [[button(menu, '❌ Отменить', 'button_admin_cancel')], [button(menu, '🏠 Главное меню', 'admin_section_main')]]); } const url = extractUrl(input); if (!url) return screen(menu, 'buttons_clean_need_url', '➕ Добавление кнопки', ['Нужна ссылка в формате https://...', '', 'Пришлите ссылку ещё раз или отмените черновик.'], [[button(menu, '❌ Отменить', 'button_admin_cancel')], [button(menu, '🏠 Главное меню', 'admin_section_main')]]); draft.url = url; flow.draft = draft; setButtonFlow(userId, flow); return saveDraft(menu, { userId }); }
async function screenForPayload(menu, payload = {}, ctx = {}) { getTenant(ctx.userId); const action = clean(payload.action); if (action === 'admin_section_buttons') return home(menu, payload, ctx); if (action === 'button_admin_recent_posts') return picker(menu, payload, ctx); if (action === 'button_admin_channel_pick') return picker(menu, { ...payload, skipChannels: '1' }, ctx); if (action === 'button_admin_select_post') return selectPost(menu, payload, ctx); if (action === 'button_admin_show_current') return showCurrent(menu, payload, ctx); if (action === 'button_admin_start_add') return startAdd(menu, payload, ctx); if (action === 'button_admin_delete') return removeLast(menu, payload, ctx); if (action === 'button_admin_cancel') return cancel(menu, payload, ctx); return null; }
function isCleanButtonAction(action = '') { return CLEAN_BUTTON_ACTIONS.includes(clean(action)); }
module.exports = { RUNTIME, CLEAN_BUTTON_ACTIONS, isCleanButtonAction, screenForPayload, handleTextInput, listPosts, findPost };

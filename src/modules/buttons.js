'use strict';
const { makeSection } = require('./_sectionFactory');
module.exports = makeSection({ id: 'buttons', title: 'Кнопки', icon: '🔘', order: 30, feature: 'buttons.enabled', description: 'CTA-кнопки будут перенесены на чистую таблицу ak_post_buttons и единый postAddonManager.' });

'use strict';
const http=require('http');
const {spawn}=require('child_process');
const {URL}=require('url');
const fs=require('fs');
const path=require('path');

const RUNTIME='SP40.4';
const SOURCE='adminkit-SP40.4-features-1-3-prod-check-fix1';
const BASE_FILE=process.env.ADMINKIT_BASE_FILE_404||'media-core-sp403.txt';
const PORT=Number(process.env.PORT||3000);
const INNER=Number(process.env.ADMINKIT_404_INNER_PORT||(PORT===3000?3300:PORT+3));
const ADMIN=process.env.DEBUG_ADMIN_TOKEN||process.env.GIFT_ADMIN_TOKEN||process.env.ADMIN_TOKEN||'admin';
const startedAt=new Date().toISOString();
const stats={proxied:0,patchedHtml:0,patchedJs:0,proxyErrors:0,childRestarts:0,policyRequests:0,lastError:null,exports:0,lastExport:null};

function env(k,d=''){return String(process.env[k]??d)}
function cleanToken(v){let s=String(v||'').trim();s=s.replace(/^[ '\"]+|[ '\"]+$/g,'').trim();s=s.replace(/^Bearer\s+/i,'').trim();s=s.replace(/[\r\n\t ]+/g,'');return s}
function mask(s){s=String(s||'');return s?{present:true,length:s.length,prefix:s.slice(0,8),suffix:s.slice(-4)}:{present:false,length:0}}
function headers(){return {'cache-control':'no-store, no-cache, must-revalidate, proxy-revalidate','pragma':'no-cache','expires':'0','x-adminkit-runtime':RUNTIME}}
function text(res,code,s,ct='text/plain; charset=utf-8'){res.writeHead(code,{'content-type':ct,...headers()});res.end(s)}
function json(res,code,o){const b=Buffer.from(JSON.stringify(o));res.writeHead(code,{'content-type':'application/json; charset=utf-8','content-length':String(b.length),...headers()});res.end(b)}
function isAdmin(req){const u=new URL(req.url,'http://x');return String(u.searchParams.get('token')||'')===String(ADMIN)}
function guard(req,res,fn){if(!isAdmin(req))return json(res,403,{ok:false,error:'forbidden',runtimeVersion:RUNTIME});return fn()}
function ghCfg(){const raw=env('GITHUB_DEBUG_TOKEN',''),tok=cleanToken(raw);return{repo:env('GITHUB_DEBUG_REPO',''),branch:env('GITHUB_DEBUG_BRANCH','main'),path:env('GITHUB_DEBUG_PATH','debug/latest.json'),litePath:env('GITHUB_DEBUG_LITE_PATH','debug/latest-lite.json'),rawToken:mask(raw),normalizedToken:mask(tok),tokenChanged:raw!==tok,token:tok}}

const FEATURE_PLAN=[
 {id:1,key:'channel_connection',title:'Подключение канала',status:'prod-check'},
 {id:2,key:'post_comments',title:'Комментарии под постами',status:'prod-check'},
 {id:3,key:'photo_comments_paid',title:'Фото в комментариях',status:'paid-prod-check'}
];
function flags(){return{sectionsActive:'1,2,3',oldCenterCta:'hidden',floatingCta:'enabled',customAttachmentMenu:'photo_only',commentVideo:'disabled',commentFiles:'disabled',commentPhoto:'enabled',commentPhotoPaidOnly:'enabled',tariffMode:env('ADMINKIT_TARIFF_MODE','paid')}}
function qaObj(){const g=ghCfg(),f=flags();return{ok:true,runtime:RUNTIME,runtimeVersion:RUNTIME,sourceMarker:SOURCE,generatedAt:Date.now(),generatedAtIso:new Date().toISOString(),startedAt,baseFile:BASE_FILE,innerPort:INNER,verdict:'PROD_CHECK_READY',safe:true,featurePlan:FEATURE_PLAN,featureFlags:f,productionRules:{commentsAttachments:'photo_only',videoInComments:false,filesInComments:false,customAttachmentMenu:'photo_only',photoCommentsPaidOnly:true,floatingCta:true,oldCenterCta:false,technicalUserToasts:false},stats,githubExport:{repo:g.repo,branch:g.branch,path:g.path,litePath:g.litePath,rawToken:g.rawToken,normalizedToken:g.normalizedToken,tokenChanged:g.tokenChanged,exports:stats.exports,lastExport:stats.lastExport},sourceOfTruth:'/debug/qa-lite?token=admin'}}
function qaLite(){const q=qaObj(),f=q.featureFlags;return[`OK: ${q.verdict}`,`runtime: ${RUNTIME}`,`sourceMarker: ${SOURCE}`,`base: ${BASE_FILE}`,`sectionsActive: ${f.sectionsActive}`,`floatingCta: ${f.floatingCta}`,`oldCenterCta: ${f.oldCenterCta}`,`customAttachmentMenu: ${f.customAttachmentMenu}`,`commentPhoto: ${f.commentPhoto}`,`commentPhotoPaidOnly: ${f.commentPhotoPaidOnly}`,`commentVideo: ${f.commentVideo}`,`commentFiles: ${f.commentFiles}`,`tariffMode: ${f.tariffMode}`,`patchedHtml: ${stats.patchedHtml}`,`patchedJs: ${stats.patchedJs}`,`proxyErrors: ${stats.proxyErrors}`,`lastError: ${stats.lastError||'none'}`,`sourceOfTruth: /debug/qa-lite?token=admin`].join('\n')+'\n'}
async function putFile(repo,branch,filePath,content,message,token){const api='https://api.github.com/repos/'+repo+'/contents/'+encodeURIComponent(filePath).replace(/%2F/g,'/');const hdr={authorization:'Bearer '+token,'content-type':'application/json','user-agent':'adminkit-debug-export'};let sha=null;try{const r=await fetch(api+'?ref='+encodeURIComponent(branch),{headers:hdr});if(r.ok){const j=await r.json();sha=j.sha||null}}catch(_){}const body={message,branch,content:Buffer.from(content).toString('base64')};if(sha)body.sha=sha;const r=await fetch(api,{method:'PUT',headers:hdr,body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error('github_put_'+r.status+':'+JSON.stringify(j).slice(0,220));return j}
async function doExport(){const g=ghCfg();if(!g.token||!g.repo)throw new Error('github_debug_env_missing');await putFile(g.repo,g.branch,g.path,JSON.stringify(qaObj(),null,2),'Update Adminkit SP40.4 fix1 QA debug',g.token);await putFile(g.repo,g.branch,g.litePath,qaLite(),'Update Adminkit SP40.4 fix1 QA lite debug',g.token);stats.exports++;stats.lastExport={atIso:new Date().toISOString(),repo:g.repo,branch:g.branch,path:g.path,litePath:g.litePath};return stats.lastExport}

const CLIENT_FIX_JS=`(function(){if(window.__ADMINKIT_SP404_FIX1__)return;window.__ADMINKIT_SP404_FIX1__=1;var T='${env('ADMINKIT_TARIFF_MODE','paid')}'.toLowerCase();function tx(n){try{return((n.innerText||n.textContent||'')+' '+(n.getAttribute&&n.getAttribute('aria-label')||'')+' '+(n.title||'')).toLowerCase()}catch(e){return''}}function css(){var s=document.createElement('style');s.textContent='.adminkit-old-center-cta{display:none!important}.ak-photo-upsell{position:fixed;left:18px;right:18px;bottom:104px;z-index:99999;background:rgba(255,255,255,.92);border:1px solid rgba(255,255,255,.9);border-radius:22px;padding:14px 16px;box-shadow:0 18px 48px rgba(20,25,40,.18);font:15px/1.25 -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial;color:#172033}.ak-photo-upsell b{display:block;margin-bottom:4px}.ak-photo-upsell button{margin-top:10px;border:0;border-radius:14px;background:#198cff;color:white;padding:9px 12px;font-weight:700}.ak-attach-photo-menu{position:fixed;left:16px;bottom:94px;z-index:99998;min-width:245px;background:rgba(255,255,255,.96);border-radius:20px;overflow:hidden;box-shadow:0 18px 48px rgba(20,25,40,.18);font:18px/1.2 -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial}.ak-attach-photo-menu button{display:flex;width:100%;gap:12px;align-items:center;justify-content:space-between;padding:15px 18px;border:0;background:transparent;color:#111;text-align:left;font:inherit}.ak-attach-photo-menu button+button{border-top:1px solid rgba(0,0,0,.08)}';document.head.appendChild(s)}function isOldCta(n){var t=tx(n);return /подарок автора|услуги автора/.test(t)}function isTechToast(n){var t=tx(n);return /pdf\/файлы|mvp доступны|видео — в посте канала|текст и фото/.test(t)}function cleanup(){document.querySelectorAll('button,a,div,span').forEach(function(n){if(isOldCta(n)){var p=n.closest&&n.closest('button,a,div');if(p)p.classList.add('adminkit-old-center-cta')}if(isTechToast(n)){var p=n.closest&&n.closest('div');if(p)p.remove()}});document.querySelectorAll('button,[role=button],li,div').forEach(function(n){var t=tx(n);if(/выбрать файл|файл|документ|video|видео/.test(t)&&!/фото/.test(t)){n.style.display='none'}if(/снять фото или видео/.test(t)){n.childNodes.forEach(function(c){if(c.nodeType===3)c.nodeValue=c.nodeValue.replace('Снять фото или видео','Снять фото')});}})}function upsell(){document.querySelectorAll('.ak-photo-upsell').forEach(function(x){x.remove()});var d=document.createElement('div');d.className='ak-photo-upsell';d.innerHTML='<b>Фото в комментариях</b>Доступно на расширенном тарифе. На бесплатном тарифе можно оставлять текстовые комментарии.<br><button type="button">Подключить</button>';document.body.appendChild(d);setTimeout(function(){try{d.remove()}catch(e){}},4200)}function menu(){document.querySelectorAll('.ak-attach-photo-menu').forEach(function(x){x.remove()});if(T==='free')return upsell();var m=document.createElement('div');m.className='ak-attach-photo-menu';m.innerHTML='<button type="button" data-ak-photo="library"><span>Выбрать фото</span><span>🖼</span></button><button type="button" data-ak-photo="camera"><span>Снять фото</span><span>📷</span></button>';document.body.appendChild(m);setTimeout(function(){document.addEventListener('click',function once(e){if(!m.contains(e.target)){m.remove();document.removeEventListener('click',once,true)}},true)},0);m.onclick=function(e){var b=e.target.closest('button');if(!b)return;var input=document.querySelector('input[type=file][accept*=image],input[type=file]');if(input){try{input.setAttribute('accept','image/*');input.removeAttribute('multiple');input.click()}catch(_){}}m.remove()}}document.addEventListener('click',function(e){var n=e.target&&e.target.closest&&e.target.closest('button,[role=button],a,label');if(!n)return;var t=tx(n);if(/выбрать файл|файл|документ|video|видео/.test(t)){e.preventDefault();e.stopPropagation();if(e.stopImmediatePropagation)e.stopImmediatePropagation();return false}if(/📎|скреп|прикреп|attachment/.test(t)){e.preventDefault();e.stopPropagation();if(e.stopImmediatePropagation)e.stopImmediatePropagation();menu();return false}},true);css();cleanup();setInterval(cleanup,1000)})();`;
function patchBody(body,headers,pathName){const ct=String(headers['content-type']||'').toLowerCase();let s=body.toString('utf8');if((ct.includes('text/html')||pathName==='/'||pathName.includes('/app'))&&!s.includes('__ADMINKIT_SP404_FIX1__')){stats.patchedHtml++;return Buffer.from(s.replace(/<\/body>/i,'<script>'+CLIENT_FIX_JS+'</script></body>'))}if((pathName.endsWith('/app.js')||pathName==='/public/app.js'||ct.includes('javascript'))&&!s.includes('__ADMINKIT_SP404_FIX1__')){stats.patchedJs++;return Buffer.from(s+'\n;'+CLIENT_FIX_JS+'\n')}return body}
function proxy(req,res){return new Promise(resolve=>{const inner=http.request({hostname:'127.0.0.1',port:INNER,method:req.method,path:req.url,headers:{...req.headers,host:'127.0.0.1:'+INNER},timeout:35000},r=>{const a=[];r.on('data',c=>a.push(c));r.on('end',()=>{let b=Buffer.concat(a),h={...r.headers,'x-adminkit-runtime':RUNTIME};delete h['transfer-encoding'];delete h.connection;try{b=patchBody(b,h,new URL(req.url,'http://x').pathname)}catch(e){stats.lastError='patch:'+e.message}h['content-length']=String(b.length);res.writeHead(r.statusCode||200,h);res.end(b);resolve()})});inner.on('timeout',()=>inner.destroy(new Error('proxy_timeout')));inner.on('error',e=>{stats.proxyErrors++;stats.lastError=e.message;json(res,502,{ok:false,runtimeVersion:RUNTIME,error:'proxy_failed',message:e.message});resolve()});req.pipe(inner)})}
let child=null,stopping=false;function startChild(){const filePath=path.join(process.cwd(),BASE_FILE);if(!fs.existsSync(filePath)){stats.lastError='base_file_missing:'+BASE_FILE;return}child=spawn(process.execPath,[filePath],{env:{...process.env,PORT:String(INNER),ADMINKIT_PARENT_RUNTIME:RUNTIME,FEATURE_ATTACHMENT_MENU:'1',ATTACHMENT_MENU_EXPERIMENT:'0',FEATURE_COMMENT_VIDEO:'0',FEATURE_COMMENT_FILES:'0'},stdio:['ignore','inherit','inherit']});child.on('exit',(code,signal)=>{child=null;if(!stopping){stats.childRestarts++;stats.lastError='child_exit:'+code+':'+signal;setTimeout(startChild,1200)}})}
process.on('SIGTERM',()=>{stopping=true;if(child)child.kill('SIGTERM');process.exit(0)});process.on('SIGINT',()=>{stopping=true;if(child)child.kill('SIGINT');process.exit(0)});startChild();
http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://x'),p=u.pathname;if(p==='/api/comments/attachment-policy'||p==='/api/attachment-policy'){stats.policyRequests++;return json(res,200,{ok:true,runtimeVersion:RUNTIME,policy:{free:'text_only_with_photo_upsell',paid:'photo_only_custom_menu',video:false,files:false,customMenu:'photo_only',photoPaidOnly:true}})}if(p==='/public/build-marker.txt'||p==='/debug/runtime-marker-sp404')return text(res,200,`${SOURCE}\n${RUNTIME}\nbase=${BASE_FILE}\ninner=${INNER}\n`);if(p==='/debug/feature-plan'||p==='/debug/features-1-3')return guard(req,res,()=>json(res,200,{ok:true,runtimeVersion:RUNTIME,sourceMarker:SOURCE,featurePlan:FEATURE_PLAN,featureFlags:flags()}));if(p==='/debug/qa-lite'||p==='/debug/sp404')return guard(req,res,()=>text(res,200,qaLite()));if(p==='/debug/qa'||p==='/debug/qa-sp404'||p==='/debug/qa-proxy')return guard(req,res,()=>json(res,200,qaObj()));if(p==='/debug/github-export-check')return guard(req,res,()=>json(res,200,{ok:true,runtimeVersion:RUNTIME,githubExport:qaObj().githubExport}));if(p==='/debug/export-sp404'||p==='/debug/export')return guard(req,res,async()=>{try{return json(res,200,{ok:true,runtimeVersion:RUNTIME,sourceMarker:SOURCE,export:await doExport()})}catch(e){stats.lastError='export:'+e.message;return json(res,500,{ok:false,runtimeVersion:RUNTIME,error:'github_export_failed',message:e.message,githubExport:qaObj().githubExport})}});stats.proxied++;return proxy(req,res)}catch(e){stats.proxyErrors++;stats.lastError=e.message;return json(res,500,{ok:false,runtimeVersion:RUNTIME,error:e.message})}}).listen(PORT,()=>console.log(`${SOURCE} listening on ${PORT}, inner ${INNER}, base ${BASE_FILE}`));

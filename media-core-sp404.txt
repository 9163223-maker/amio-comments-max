'use strict';
const http=require('http');
const {spawn}=require('child_process');
const {URL}=require('url');
const fs=require('fs');
const path=require('path');
const RUNTIME='SP40.5.4d';
const SOURCE='adminkit-SP40.5.4d-storage-debug';
const BASE_FILE=process.env.ADMINKIT_BASE_FILE_404||'media-core-sp403.txt';
const PORT=Number(process.env.PORT||3000);
const INNER=Number(process.env.ADMINKIT_404_INNER_PORT||(PORT===3000?3300:PORT+3));
const ADMIN=process.env.DEBUG_ADMIN_TOKEN||process.env.GIFT_ADMIN_TOKEN||process.env.ADMIN_TOKEN||'admin';
const DB_URL=process.env.DATABASE_URL||process.env.POSTGRES_URI||process.env.NF_ADMINKIT_POSTGRES_PROD_POSTGRES_URI||'';
const PG_TABLE=(process.env.ADMINKIT_PG_TABLE||'adminkit_store_kv').replace(/[^a-zA-Z0-9_]/g,'')||'adminkit_store_kv';
const startedAt=new Date().toISOString();
const stats={proxied:0,patched:0,patchedStartCta:0,cta:0,policy:0,proxyErrors:0,childRestarts:0,storageChecks:0,lastStorageCheck:null,lastStorageError:null,lastError:null};
let pgPool=null;
function head(){return {'cache-control':'no-store, no-cache, must-revalidate','pragma':'no-cache','expires':'0','x-adminkit-runtime':RUNTIME}}
function text(res,code,s){res.writeHead(code,{'content-type':'text/plain; charset=utf-8',...head()});res.end(s)}
function json(res,code,o){const b=Buffer.from(JSON.stringify(o));res.writeHead(code,{'content-type':'application/json; charset=utf-8','content-length':String(b.length),...head()});res.end(b)}
function ok(req){const u=new URL(req.url,'http://x');return String(u.searchParams.get('token')||'')===String(ADMIN)}
function guard(req,res,fn){if(!ok(req))return json(res,403,{ok:false,error:'forbidden',runtimeVersion:RUNTIME});return fn()}
function nodeOptionsInfo(){const value=String(process.env.NODE_OPTIONS||'');return{hasStorePreload:value.includes('store-postgres-sp405.js'),hasStopwordHook:value.includes('stopword-hook-sp4054c.js'),length:value.length}}
function flags(){return{floatingCta:'enabled_except_start_landing',startLandingCta:'hidden',oldCenterCta:'removed_from_payload',technicalUserToasts:'disabled',customAttachmentMenu:'photo_only',commentPhoto:'enabled',commentVideo:'disabled',commentFiles:'disabled',storageDebug:'enabled',databaseUrlPresent:!!DB_URL,nodeOptions:nodeOptionsInfo(),tariffMode:process.env.ADMINKIT_TARIFF_MODE||'paid'}}
function getLocalPreloadState(){return{postgres:global.__ADMINKIT_PG_STORE__||null,stopword:global.__ADMINKIT_STOPWORD_HOOK__||null}}
function getPool(){if(!DB_URL)return null;if(pgPool)return pgPool;const {Pool}=require('pg');pgPool=new Pool({connectionString:DB_URL,ssl:{rejectUnauthorized:false},connectionTimeoutMillis:2500,statement_timeout:3500});return pgPool}
async function storageDebug(){const started=Date.now();const local=getLocalPreloadState();const info={ok:true,runtime:RUNTIME,runtimeVersion:RUNTIME,sourceMarker:SOURCE,generatedAt:Date.now(),generatedAtIso:new Date().toISOString(),databaseUrlPresent:!!DB_URL,table:PG_TABLE,nodeOptions:nodeOptionsInfo(),localPreloadState:local,postgres:{reachable:false,tableExists:false,storeRow:false,updatedAt:null,channels:0,posts:0,comments:0,moderationKeys:0,gifts:0,rawDataType:null,error:null},stats};
  stats.storageChecks++;
  if(!DB_URL){info.postgres.error='database_url_missing';stats.lastStorageError=info.postgres.error;return info}
  try{const pool=getPool();const tableExists=await pool.query('SELECT to_regclass($1) AS name',[PG_TABLE]);info.postgres.tableExists=!!tableExists.rows?.[0]?.name;if(info.postgres.tableExists){const q=`SELECT updated_at, jsonb_typeof(data) AS data_type, COALESCE(jsonb_object_length(data->'channels'),0) AS channels, COALESCE(jsonb_object_length(data->'posts'),0) AS posts, COALESCE(jsonb_object_length(data->'comments'),0) AS comments, COALESCE(jsonb_object_length(data->'moderation'),0) AS moderation_keys, COALESCE(jsonb_array_length(CASE WHEN jsonb_typeof(data->'giftCampaigns')='array' THEN data->'giftCampaigns' ELSE '[]'::jsonb END),0) AS gifts FROM ${PG_TABLE} WHERE key=$1 LIMIT 1`;const row=await pool.query(q,['store']);const r=row.rows?.[0]||null;if(r){info.postgres.storeRow=true;info.postgres.updatedAt=r.updated_at;info.postgres.rawDataType=r.data_type;info.postgres.channels=Number(r.channels||0);info.postgres.posts=Number(r.posts||0);info.postgres.comments=Number(r.comments||0);info.postgres.moderationKeys=Number(r.moderation_keys||0);info.postgres.gifts=Number(r.gifts||0)}}info.postgres.reachable=true;stats.lastStorageError=null}catch(e){info.postgres.error=e.message||String(e);stats.lastStorageError=info.postgres.error}stats.lastStorageCheck={atIso:new Date().toISOString(),ms:Date.now()-started,reachable:info.postgres.reachable,tableExists:info.postgres.tableExists,storeRow:info.postgres.storeRow,channels:info.postgres.channels,posts:info.postgres.posts,error:info.postgres.error||null};return info}
async function storageLite(){const s=await storageDebug();return[`OK: ${s.postgres.error?'WARNING':'PROD_CHECK_READY'}`,`runtime: ${RUNTIME}`,`sourceMarker: ${SOURCE}`,`databaseUrlPresent: ${s.databaseUrlPresent}`,`nodeOptions.storePreload: ${s.nodeOptions.hasStorePreload}`,`nodeOptions.stopwordHook: ${s.nodeOptions.hasStopwordHook}`,`postgres.reachable: ${s.postgres.reachable}`,`postgres.tableExists: ${s.postgres.tableExists}`,`postgres.storeRow: ${s.postgres.storeRow}`,`postgres.updatedAt: ${s.postgres.updatedAt||'none'}`,`postgres.channels: ${s.postgres.channels}`,`postgres.posts: ${s.postgres.posts}`,`postgres.comments: ${s.postgres.comments}`,`postgres.moderationKeys: ${s.postgres.moderationKeys}`,`postgres.gifts: ${s.postgres.gifts}`,`postgres.error: ${s.postgres.error||'none'}`].join('\n')+'\n'}
function qa(){return{ok:true,runtime:RUNTIME,runtimeVersion:RUNTIME,sourceMarker:SOURCE,generatedAt:Date.now(),generatedAtIso:new Date().toISOString(),startedAt,baseFile:BASE_FILE,innerPort:INNER,featureFlags:flags(),preloadState:getLocalPreloadState(),stats}}
function qaLite(){const f=flags();return[`OK: PROD_CHECK_READY`,`runtime: ${RUNTIME}`,`sourceMarker: ${SOURCE}`,`base: ${BASE_FILE}`,`floatingCta: ${f.floatingCta}`,`startLandingCta: ${f.startLandingCta}`,`oldCenterCta: ${f.oldCenterCta}`,`technicalUserToasts: ${f.technicalUserToasts}`,`customAttachmentMenu: ${f.customAttachmentMenu}`,`commentVideo: ${f.commentVideo}`,`commentFiles: ${f.commentFiles}`,`storageDebug: ${f.storageDebug}`,`databaseUrlPresent: ${f.databaseUrlPresent}`,`nodeOptionsStorePreload: ${f.nodeOptions.hasStorePreload}`,`nodeOptionsStopwordHook: ${f.nodeOptions.hasStopwordHook}`,`storageChecks: ${stats.storageChecks}`,`lastStorageError: ${stats.lastStorageError||'none'}`,`patched: ${stats.patched}`,`patchedStartCta: ${stats.patchedStartCta}`,`proxyErrors: ${stats.proxyErrors}`,`lastError: ${stats.lastError||'none'}`].join('\n')+'\n'}
function cta(){return{ok:true,enabled:true,active:true,type:'link',icon:'🐋',title:'АдминКИТ',text:'Инструменты автора',buttonText:'Открыть',button:'Открыть',url:'',ui:{variant:'compact',backgroundOpacity:0.42,hideOnStartLanding:true}}}
const START_CTA_GUARD=`<style data-ak4054a="1">body.ak-start-landing .ak-cta{display:none!important;visibility:hidden!important;pointer-events:none!important}.ak-cta{background:rgba(255,255,255,.42)!important;box-shadow:0 10px 26px rgba(20,25,40,.07)!important}</style><script data-ak4054a="1">(function(){if(window.__ak4054a)return;window.__ak4054a=1;function scan(){var s=(document.body&&document.body.innerText)||'';var start=/Перейти в чат с ботом|Сообщество\s*\/\s*новости|АдминКИТ\s*—\s*система управления MAX/.test(s);if(document.body)document.body.classList.toggle('ak-start-landing',!!start);if(start)document.querySelectorAll('.ak-cta').forEach(function(e){e.style.display='none';e.style.visibility='hidden';e.style.pointerEvents='none';});}setTimeout(scan,80);setTimeout(scan,500);setTimeout(scan,1200);setTimeout(scan,2400);if(document.body)new MutationObserver(function(){clearTimeout(window.__ak4054at);window.__ak4054at=setTimeout(scan,80)}).observe(document.body,{childList:true,subtree:true,characterData:true});})();</script>`;
function patch(buf,headers){const ct=String(headers['content-type']||'').toLowerCase();if(!ct.includes('text/html')&&!ct.includes('javascript')&&!ct.includes('json'))return buf;let s=buf.toString('utf8');const before=s;if(ct.includes('text/html')&&!s.includes('data-ak4054a')){s=s.replace(/<\/head>/i,START_CTA_GUARD+'</head>').replace(/<\/body>/i,START_CTA_GUARD+'</body>');stats.patchedStartCta++}
s=s.replace(/В комментариях MVP[^<\"']{0,260}/g,'');s=s.replace(/🎁\s*Подарок автора/g,'');s=s.replace(/Подарок автора/g,'');s=s.replace(/Услуги автора/g,'');s=s.replace(/Снять фото или видео/g,'Снять фото');s=s.replace(/Выбрать файл/g,'');if(s!==before)stats.patched++;return Buffer.from(s)}
function proxy(req,res){return new Promise(resolve=>{const r=http.request({hostname:'127.0.0.1',port:INNER,method:req.method,path:req.url,headers:{...req.headers,host:'127.0.0.1:'+INNER},timeout:35000},rr=>{const a=[];rr.on('data',c=>a.push(c));rr.on('end',()=>{let b=Buffer.concat(a),h={...rr.headers,'x-adminkit-runtime':RUNTIME};delete h['transfer-encoding'];delete h.connection;try{b=patch(b,h)}catch(e){stats.lastError='patch:'+e.message}h['content-length']=String(b.length);res.writeHead(rr.statusCode||200,h);res.end(b);resolve()})});r.on('timeout',()=>r.destroy(new Error('proxy_timeout')));r.on('error',e=>{stats.proxyErrors++;stats.lastError=e.message;json(res,502,{ok:false,runtimeVersion:RUNTIME,error:'proxy_failed',message:e.message});resolve()});req.pipe(r)})}
let child=null,stopping=false;function startChild(){const fp=path.join(process.cwd(),BASE_FILE);if(!fs.existsSync(fp)){stats.lastError='base_file_missing:'+BASE_FILE;return}child=spawn(process.execPath,[fp],{env:{...process.env,PORT:String(INNER),ADMINKIT_PARENT_RUNTIME:RUNTIME,FEATURE_ATTACHMENT_MENU:'1',FEATURE_COMMENT_VIDEO:'0',FEATURE_COMMENT_FILES:'0'},stdio:['ignore','inherit','inherit']});child.on('exit',(code,signal)=>{child=null;if(!stopping){stats.childRestarts++;stats.lastError='child_exit:'+code+':'+signal;setTimeout(startChild,1200)}})}
process.on('SIGTERM',()=>{stopping=true;if(child)child.kill('SIGTERM');try{if(pgPool)pgPool.end()}catch{}process.exit(0)});process.on('SIGINT',()=>{stopping=true;if(child)child.kill('SIGINT');try{if(pgPool)pgPool.end()}catch{}process.exit(0)});startChild();
http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://x'),p=u.pathname;if(p==='/api/promo/cta'||p==='/api/cta'||p==='/api/floating-cta'){stats.cta++;return json(res,200,cta())}if(p==='/api/comments/attachment-policy'||p==='/api/attachment-policy'){stats.policy++;return json(res,200,{ok:true,runtimeVersion:RUNTIME,policy:{paid:'photo_only_custom_menu',free:'text_only_with_photo_upsell',video:false,files:false,customMenu:'photo_only'}})}if(p==='/public/build-marker.txt'||p==='/debug/runtime-marker-sp404')return text(res,200,`${SOURCE}\n${RUNTIME}\nbase=${BASE_FILE}\ninner=${INNER}\n`);if(p==='/debug/storage-lite'||p==='/debug/db-lite')return guard(req,res,async()=>text(res,200,await storageLite()));if(p==='/debug/storage'||p==='/debug/db'||p==='/debug/migration')return guard(req,res,async()=>json(res,200,await storageDebug()));if(p==='/debug/qa-lite'||p==='/debug/sp404')return guard(req,res,()=>text(res,200,qaLite()));if(p==='/debug/qa'||p==='/debug/qa-sp404')return guard(req,res,()=>json(res,200,qa()));stats.proxied++;return proxy(req,res)}catch(e){stats.proxyErrors++;stats.lastError=e.message;return json(res,500,{ok:false,runtimeVersion:RUNTIME,error:e.message})}}).listen(PORT,()=>console.log(`${SOURCE} listening on ${PORT}, inner ${INNER}, base ${BASE_FILE}`));

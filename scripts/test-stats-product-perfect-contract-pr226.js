'use strict';

// PR258 intentionally replaces the old PR226 statistics root with a clean root menu.
// The active regression contract is now the channel/chat separation + clean root UX matrix.
require('./test-channel-chat-separation-menu-ux');
